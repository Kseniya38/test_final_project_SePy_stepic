[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    user: marker for user registration tests
    need_review: marker for tests that need review
    login_guest: marker for tests of ability to login for a guest